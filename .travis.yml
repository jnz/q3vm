language: c
addons:
  - clang
install: 
  # cppcheck
  - sudo apt-get install -qq cppcheck
  - sudo apt-get install -qq valgrind
script:
  - make valgrind
  - make analysis
  - make clean
  - make test
after_success:
  - bash <(curl -s https://codecov.io/bash)
